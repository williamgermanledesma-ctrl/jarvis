# Core (Phase 1-3): the brain, the web UI, the memory store
ollama>=0.3.0
flask>=3.0.0
werkzeug>=3.0.0         # secure_filename for uploads
chromadb>=0.5.0
requests>=2.31           # for cloud providers (Claude/Gemini) — already used by wake_listener
pytest>=8.0.0           # for the jarvis_run_tests sandbox (Write-Test-Fix loop)

# Wake word / hands-free voice (Phase 4) — only needed for wake_listener.py.
# These are the install-heavy ones; also requires: brew install portaudio ffmpeg
openwakeword>=0.6.0
sounddevice>=0.4.6
numpy>=1.24
requests>=2.31
openai-whisper>=20231117
# Smart voice activity detection + barge-in (optional, for wake_listener.py)
torch>=2.0          # Silero VAD runs on torch; CPU build is fine on Apple Silicon
gunicorn>=21.0          # production WSGI server for cloud deployment (Railway etc.)
